# Charless言語 v3 命令体系設計

## 基本方針

- **数値のみで構成**（根本思想は絶対に変えない）
- **3桁命令体系**（000-999、1000個の命令を許容）
- **2桁命令は廃止**（今後は3桁命令のみ）
- **スケーラブルな設計**（将来の拡張に対応）

## 既存命令の3桁への置き換え

### 命令マッピング表

| 旧命令（2桁） | 新命令（3桁） | 説明 |
|------------|------------|------|
| `00` | `000` | HALT |
| `01` | `010` | 文字列出力 |
| `02` | `020` | 数値出力 |
| `03` | `030` | メモリ中の文字列出力 |
| `04` | `040` | 文字出力 |
| `11` | `100` | 文字入力 |
| `12` | `101` | 数値入力 |
| `13` | `102` | 文字列入力 |
| `21` | `210` | 改行文字出力 |
| `51` | `500` | PUSH |
| `52` | `501` | POP |
| `53` | `510` | STORE |
| `54` | `511` | LOAD |
| `61` | `600` | ADD |
| `62` | `601` | SUB |
| `63` | `602` | MUL |
| `64` | `603` | DIV |
| `65` | `604` | MOD |
| `71` | `700` | JUMP |
| `72` | `701` | JZ |
| `73` | `702` | JNZ |
| `81` | `800` | EQS |
| `82` | `801` | EQN |
| `83` | `802` | GT |
| `84` | `803` | LT |
| `85` | `804` | GTE |
| `86` | `805` | LTE |
| `97` | `900` | 一行コメント |
| `98` | `901` | 複数行コメント |

### 移行例

**旧コード（2桁命令）:**
```
12205120990208320722099542001202893101311520712099672001202783111200020
```

**新コード（3桁命令）:**
```
1012050020990208022070120995420010202893101311520700209967200102027831112000020
```

**変換内容（詳細）:**
- `1220` → `10120` (数値入力)
- `512099020` → `5002099020` (PUSH 0)
- `8320` → `80220` (GT比較)
- `7220995420` → `70120995420` (JZ アドレス54)
- `01202893101311520` → `010202893101311520` ("Yes"出力)
- `7120996720` → `70020996720` (JUMP アドレス67)
- `0120278311120` → `01020278311120` ("No"出力)
- `0020` → `00020` (HALT)

**注意**: 数値リテラル（`99`で始まる）とセパレータ（`20`）は変更しません。

## 命令体系の役割分担

### 000-099: 基本・システム領域（100個）

- **000-009**: 終了・システム系（10個）
  - `000`: HALT（旧`00`から移行）
  - `001-009`: 予約（システム拡張用）

- **010-049**: 出力系（40個）
  - `010`: 文字列出力（旧`01`から移行）
  - `020`: 数値出力（旧`02`から移行）
  - `030`: メモリ中の文字列出力（旧`03`から移行）
  - `040`: 文字出力（旧`04`から移行）
  - `015-019`: 予約
  - `021-029`: 予約
  - `031-039`: 予約
  - `041-049`: 拡張出力（ファイル出力、フォーマット出力など）

- **050-099**: 予約・将来拡張用（50個）

### 100-199: 入力系（100個）

- **100-109**: 基本入力（10個）
  - `100`: 文字入力（旧`11`から移行）
  - `101`: 数値入力（旧`12`から移行）
  - `102`: 文字列入力（旧`13`から移行）
  - `103-109`: 拡張入力（ファイル入力、型付き入力など）

- **110-199**: 入力検証・変換系（90個）
  - `110-119`: 入力検証（実数判定、整数判定など）
  - `120-129`: 型変換
  - `130-199`: 予約

### 200-299: 型システム（100個）

- **200-209**: 型定義・宣言（10個）
  - `200`: 型チェック開始
  - `201`: 整数型マーカー
  - `202`: 実数型マーカー
  - `203`: 文字列型マーカー
  - `204`: 文字型マーカー
  - `205-209`: 予約

- **210-219**: 特殊出力（10個）
  - `210`: 改行文字出力（旧`21`から移行）
  - `211-219`: 予約

- **220-249**: 型変換（30個）
  - `220-229`: 整数関連変換
  - `230-239`: 実数関連変換
  - `240-249`: 文字列関連変換

- **250-299**: 型操作・検証（50個）
  - `250-259`: 型チェック命令
  - `260-299`: 予約

### 300-399: 関数・サブルーチン系（100個）

- **300-309**: 関数定義（10個）
  - `300`: 関数定義開始
  - `301`: 関数定義終了
  - `302`: 関数パラメータ定義
  - `303-309`: 予約

- **310-349**: 関数呼び出し（40個）
  - `310`: 関数呼び出し
  - `311`: 戻り値取得
  - `312-349`: 拡張呼び出し（可変引数、名前付き引数など）

- **350-399**: 関数管理（50個）
  - `350-359`: 関数テーブル操作
  - `360-399`: 予約

### 400-499: 文字列操作拡張（100個）

- **400-409**: 基本文字列操作（10個）
  - `400`: 文字列長取得
  - `401`: 文字列結合
  - `402`: 部分文字列取得
  - `403`: 文字列検索（文字検索、部分文字列検索）
  - `404`: 文字列置換
  - `405-409`: 予約

- **410-449**: 文字列変換（40個）
  - `410-419`: 数値→文字列
  - `420-429`: 文字列→数値
  - `430-449`: 予約

- **450-499**: 高度な文字列操作（50個）
  - `450-459`: 正規表現風操作
  - `460-499`: 予約

### 500-599: スタック・メモリ拡張（100個）

- **500-509**: 基本スタック操作（10個）
  - `500`: PUSH（旧`51`から移行）
  - `501`: POP（旧`52`から移行）
  - `502`: DUP（スタックトップを複製）
  - `503`: SWAP（スタック上2つの値を交換）
  - `504`: ROT（スタック上3つの値を回転）
  - `505-509`: 予約

- **510-549**: メモリ操作拡張（40個）
  - `510`: STORE（旧`53`から移行）
  - `511`: LOAD（旧`54`から移行）
  - `512-519`: メモリ範囲操作
  - `520-549`: 予約

- **550-599**: 高度なメモリ操作（50個）
  - `550-559`: 動的メモリ管理
  - `560-599`: 予約

### 600-699: 算術演算拡張（100個）

- **600-609**: 基本算術（10個）
  - `600`: ADD（旧`61`から移行）
  - `601`: SUB（旧`62`から移行）
  - `602`: MUL（旧`63`から移行）
  - `603`: DIV（旧`64`から移行）
  - `604`: MOD（旧`65`から移行）
  - `605-609`: 予約

- **610-649**: 実数演算（40個）
  - `610-619`: 実数加減乗除
  - `620-629`: 数学関数（sin, cos, sqrt, logなど）
  - `630-649`: 予約

- **650-699**: ビット演算・論理演算（50個）
  - `650-659`: ビット演算（AND, OR, XOR, NOT, SHIFT）
  - `660-669`: 論理演算
  - `670-699`: 予約

### 700-799: 制御構造拡張（100個）

- **700-709**: 基本制御（10個）
  - `700`: JUMP（旧`71`から移行）
  - `701`: JZ（旧`72`から移行）
  - `702`: JNZ（旧`73`から移行）
  - `703-709`: 予約

- **710-749**: ループ構造（40個）
  - `710-719`: FORループ
  - `720-729`: WHILEループ
  - `730-749`: 予約

- **750-799**: 例外処理・エラーハンドリング（50個）
  - `750-759`: 例外処理
  - `760-799`: 予約

### 800-899: 比較演算拡張（100個）

- **800-809**: 基本比較（10個）
  - `800`: EQS（旧`81`から移行）
  - `801`: EQN（旧`82`から移行）
  - `802`: GT（旧`83`から移行）
  - `803`: LT（旧`84`から移行）
  - `804`: GTE（旧`85`から移行）
  - `805`: LTE（旧`86`から移行）
  - `806-809`: 予約

- **810-849**: 型付き比較（40個）
  - `810-819`: 実数比較
  - `820-829`: 文字列比較拡張
  - `830-849`: 予約

- **850-899**: 高度な比較（50個）
  - `850-859`: パターンマッチング
  - `860-899`: 予約

### 900-999: その他・予約領域（100個）

- **900-909**: コメント（10個）
  - `900`: 一行コメント（旧`97`から移行）
  - `901`: 複数行コメント（旧`98`から移行）
  - `902-909`: 予約

- **910-949**: デバッグ・開発支援（40個）
  - `910-919`: デバッグ出力
  - `920-949`: 予約

- **950-999**: 予約領域（50個）
  - 将来の拡張用

## 移行ガイド

### 2桁命令の廃止

**重要**: v3では2桁命令は完全に廃止されます。既存のコードは手動で3桁命令に変換する必要があります。

### 移行手順

1. 既存の`.cless`ファイルを開く
2. 上記のマッピング表を参照して、すべての2桁命令を3桁命令に置き換える
3. 数値リテラル（`99`で始まる）は変更不要
4. セパレータ（`20`）は変更不要

### 移行ツール（将来実装予定）

将来的には、2桁命令を3桁命令に自動変換する移行ツールを提供する予定です。

## 実装の優先順位

### Phase 1: 基本拡張（必須）
1. 3桁命令のパース実装
2. 既存命令の3桁への置き換え（000, 010-040, 100-102, 210, 500-501, 510-511, 600-604, 700-702, 800-805, 900-901）
3. 2桁命令の検出とエラー出力（移行を促す）
4. 型システム基本（200-209）
5. 関数定義基本（300-309）

### Phase 2: 実用機能
1. 文字列操作拡張（400-409）
2. 実数演算（610-619）
3. 入力検証（110-119）

### Phase 3: 高度な機能
1. ループ構造（710-729）
2. 例外処理（750-759）
3. 動的メモリ管理（550-559）

## 設計の利点

1. **スケーラビリティ**: 1000個の命令で将来の拡張に対応
2. **一貫性**: すべての命令が3桁で統一され、パースが簡潔になる
3. **体系的な分類**: 機能ごとに100個ずつ割り当てで管理しやすい
4. **拡張性**: 各カテゴリに予約領域を確保
5. **型安全性**: 型システムの導入で実数判定などが可能に
6. **明確性**: 2桁命令の曖昧さを排除

## 注意点

- 数値のみの制約を維持
- **命令は必ず3桁**（2桁命令はエラー）
- 命令の解釈は左から順に（3桁を読み取る）
- `20`（セパレータ）と`99`（数値リテラル）は引き続き予約語
- 既存のコードは手動で3桁命令に変換する必要がある

